## Summary

<!-- One or two sentences describing the change. -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Refactor / chore
- [ ] Docs

## MCP server changes

<!-- Only relevant if the PR touches the MCP endpoint. Otherwise delete. -->

- Tools added/changed: `...`
- Scopes affected: `...`
- Protocol compatibility: `...`

## Tests

- [ ] `vendor/bin/pint --test` passes
- [ ] `vendor/bin/phpstan analyse` passes
- [ ] `php artisan test` passes
- [ ] `npm run types:check` and `npm run build` pass

## Manual verification

<!-- Briefly note any manual smoke steps performed (e.g. live tool call with a scoped token). -->